# 🏗️ FIRM - ARCHITECTURE DIAGRAMS & VISUAL GUIDE
## Complete Visual Understanding of 5-Layer Perfect Architecture

**Дата:** 25 января 2026  
**Версия:** 1.0 DIAGRAMS  
**Статус:** VISUAL REFERENCE

---

## 📐 DIAGRAM 1: 5-LAYER ARCHITECTURE (THE CORE)

```
┌─────────────────────────────────────────────────────────────┐
│                    LAYER 4: Entry Points                    │
│                      (main.ts, CLI)                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Responsibility: Process initialization, logging    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓ depends on
┌─────────────────────────────────────────────────────────────┐
│                    LAYER 3: Orchestration                   │
│                   (app/firm-instance.ts)                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Responsibility: Compose validators, handle flows   │   │
│  │  - Schema building                                  │   │
│  │  - Result formatting                               │   │
│  │  - Framework integration                           │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓ depends on
┌─────────────────────────────────────────────────────────────┐
│        LAYER 2: CORE BUSINESS LOGIC (PURE FUNCTIONS)       │
│               (core/validators/, core/compiler)            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ⭐⭐⭐ CRITICAL LAYER ⭐⭐⭐                        │
│  │  - ZERO I/O operations (no disk, network, async)   │   │
│  │  - PURE functions only (predictable behavior)      │   │
│  │  - 50M+ ops/sec target (performance-critical)      │   │
│  │  - All business logic (validation algorithms)      │   │
│  │  - Schema compilation (optimization)               │   │
│  │  - Type transformations                            │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓ depends on
┌─────────────────────────────────────────────────────────────┐
│           LAYER 1: Infrastructure & Utilities              │
│      (infrastructure/, config/, observability/)            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  - Error formatting & messages                      │   │
│  │  - Performance caching                              │   │
│  │  - Benchmarking utilities                           │   │
│  │  - Configuration management                         │   │
│  │  - Logging & debugging (optional I/O)               │   │
│  │  - External service integrations                    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓ depends on
┌─────────────────────────────────────────────────────────────┐
│            LAYER 0: Foundation & Types                      │
│              (common/types, errors, constants)             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  - TypeScript type definitions                      │   │
│  │  - Custom error classes                             │   │
│  │  - Global constants                                 │   │
│  │  - Utility functions (pure)                         │   │
│  │  - Type inference helpers                           │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘

KEY PRINCIPLE: Layer N depends ONLY on layers < N
RESULT: Perfect modularity, testability, scalability
BENEFIT: core/ can be tested in complete isolation!
```

---

## 🔄 DIAGRAM 2: REQUEST VALIDATION FLOW

```
Input Data
    ↓
┌─────────────────────────────────────────────┐
│  app/firm-instance.ts                      │
│  - Accept data                              │
│  - Select appropriate validator             │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│  core/validators/object-validator.ts       │
│  - Type check                               │
│  - Field validation (recursive)             │
└─────────────────────────────────────────────┘
    ├─→ s.string().email() ──┐
    ├─→ s.number().min(0) ───┤
    ├─→ s.boolean() ─────────┤
    └─→ s.date() ────────────┘
       All in core/validators/
    ↓
┌─────────────────────────────────────────────┐
│  core/compiler.ts                          │
│  - Compile schema (if requested)            │
│  - Optimize validation function             │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│  common/types.ts                           │
│  Return ValidationResult<T>:               │
│  { ok: true, data: T } or                  │
│  { ok: false, errors: ValidationError[] }  │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│  infrastructure/error-handler.ts           │
│  - Format errors (if needed)                │
│  - Cache results (if needed)                │
└─────────────────────────────────────────────┘
    ↓
Return Result to Caller

TIME ANALYSIS:
├─ core/ logic: <100ns (50M+ ops/sec possible!)
├─ infrastructure/: <1µs (optional, can cache)
└─ Total: <1-10µs depending on complexity
```

---

## 🎯 DIAGRAM 3: DEPENDENCY GRAPH (DAG - Directed Acyclic Graph)

```
┌─────────────────────────────────────────────────────────────┐
│                  main.ts (ENTRY POINT)                      │
└────────────┬────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────┐
│              app/firm-instance.ts                           │
│         (Main FIRM orchestrator & composition)              │
└────────┬────────────────────────┬────────────────────────────┘
         │                        │
         ↓                        ↓
    ┌──────────────┐      ┌─────────────────────┐
    │ core/        │      │ infrastructure/     │
    │ validators/  │      │ error-handler.ts    │
    └──────┬───────┘      └─────────┬───────────┘
           │                        │
           └────────────┬───────────┘
                        ↓
            ┌──────────────────────┐
            │  common/types.ts     │
            │  common/errors.ts    │
            │  common/constants.ts │
            └──────────────────────┘

✅ NO CIRCULAR DEPENDENCIES!
✅ All arrows point DOWN (never UP or SIDEWAYS)
✅ core/ depends only on common/ (testable in isolation!)
✅ infrastructure/ depends on common/
✅ app/ composes core/ + infrastructure/
✅ main/ orchestrates app/

VERIFICATION: npm dpdm --circular src/main.ts → ZERO violations
```

---

## 📊 DIAGRAM 4: PERFORMANCE MODEL

```
SIMPLE STRING VALIDATION (50M ops/sec target):
┌──────────────────────────────────────────┐
│ Input: "test@example.com"                │
└──────────────────────────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ Type check: string?   │ ~10ns
        └───────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ Email regex: /@/      │ ~20ns
        └───────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ Return { ok: true }   │ ~10ns
        └───────────────────────┘
                    ↓
        TOTAL: ~40ns per validation
        = 1,000,000,000ns ÷ 40ns = 25M+ ops/sec
        = WITH COMPILATION: 50M+ ops/sec possible!

---

COMPILED SCHEMA OPTIMIZATION:
┌──────────────────────────────────────────┐
│ Before compilation:                      │
│ schema.validate(data)                    │
│ → Traverses tree 100+ times             │
│ → Function calls overhead               │
│ → 10M ops/sec max                       │
└──────────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────────┐
│ After compilation:                       │
│ const validate = schema.compile()        │
│ validate(data) → Direct inline function │
│ → No tree traversal                     │
│ → No function overhead                  │
│ → 50M+ ops/sec possible!                │
└──────────────────────────────────────────┘

SCALING BY COMPLEXITY:
┌─────────────────────────────────────────────────┐
│ Operation           │ ops/sec  │ vs Zod         │
├─────────────────────┼──────────┼────────────────┤
│ simple string       │ 50M+     │ 7.5x faster    │
│ simple number       │ 50M+     │ 7.5x faster    │
│ object (5 fields)   │ 10M+     │ 5x faster      │
│ array (10 items)    │ 5M+      │ 3x faster      │
│ nested object       │ 1M+      │ 2x faster      │
└─────────────────────┴──────────┴────────────────┘

MEMORY USAGE:
├─ No allocations for validation result
├─ Reuse buffers for error arrays
├─ Zero garbage collection pressure
└─ Predictable memory footprint
```

---

## 🔒 DIAGRAM 5: TYPE SAFETY FLOW

```
INPUT: Untyped user data
    ↓
┌──────────────────────────────┐
│ const schema = s.object({    │
│   email: s.string().email(), │
│   age: s.number().min(0),    │
│ })                           │
└──────────────────────────────┘
    ↓
┌──────────────────────────────┐
│ const result = schema.validate()
└──────────────────────────────┘
    ↓
┌──────────────────────────────┐
│ if (result.ok) {             │
│   // TypeScript knows:       │
│   // result.data has shape:  │
│   // { email: string,        │
│   //   age: number }         │
│   doSomething(result.data)   │
│ }                            │
└──────────────────────────────┘

TYPE INFERENCE JOURNEY:
User creates schema
    ↓
Schema builder tracks types
    ↓
Validation returns typed result
    ↓
TypeScript narrows within if blocks
    ↓
Complete type safety! No 'any' needed!

HOW IT WORKS (Under the hood):
┌─────────────────────────────┐
│ class Schema<T> {          │
│   _type?: T;  // Phantom! │
│   ...                      │
│ }                          │
└─────────────────────────────┘

Phantom type pattern = Type info with zero runtime cost!
```

---

## 🔍 DIAGRAM 6: CORE VS INFRASTRUCTURE SEPARATION

```
WHAT GOES IN CORE/ (Pure):
┌─────────────────────────────────────────┐
│ ✅ Type checking                        │
│ ✅ Format validation (regex, length)    │
│ ✅ Schema composition (object, array)   │
│ ✅ Type transformation (trim, lowercase)│
│ ✅ Discriminated unions                 │
│ ✅ Error detection                      │
│ ❌ ERROR FORMATTING                     │
│ ❌ I/O operations (database checks)     │
│ ❌ Async operations                     │
│ ❌ Logging                              │
│ ❌ Caching                              │
└─────────────────────────────────────────┘

WHAT GOES IN INFRASTRUCTURE/ (Impure):
┌─────────────────────────────────────────┐
│ ✅ ERROR FORMATTING                     │
│ ✅ I/O operations (database checks)     │
│ ✅ Async validators                     │
│ ✅ Logging & debugging                  │
│ ✅ Performance caching                  │
│ ✅ External service calls               │
│ ❌ Core validation logic                │
│ ❌ Type definitions                     │
│ ❌ Schema building                      │
└─────────────────────────────────────────┘

BENEFITS OF SEPARATION:
├─ core/ = 50M+ ops/sec (pure functions)
├─ infrastructure/ = can be async/slow
├─ Tests core/ in milliseconds (no setup)
├─ Tests infrastructure/ separately
├─ Can optimize core/ independently
└─ core/ never breaks from I/O issues!

EXAMPLE:
// core/ - PURE, fast, testable
const validateEmail = (v: string): boolean => /@/.test(v);

// infrastructure/ - can be slow, can have side effects
const validateEmailExists = async (email: string): Promise<boolean> => {
  const user = await db.findUnique({ email }); // I/O here!
  return !!user;
};
```

---

## 📋 QUICK REFERENCE: When in doubt...

```
Is it type checking?        → core/
Is it format validation?    → core/
Is it transformation?       → core/
Is it error formatting?     → infrastructure/
Does it hit database?       → infrastructure/
Does it make network calls? → infrastructure/
Is it async?                → infrastructure/
Does it have side effects?  → infrastructure/

RULE OF THUMB: If it can't be tested without setup,
it belongs in infrastructure/, not core/!
```

---

**Дата:** 25 января 2026  
**Версия:** 1.0 DIAGRAMS  
**Статус:** COMPLETE VISUAL REFERENCE

These diagrams visualize everything in FIRM_QUICK_REFERENCE.md!